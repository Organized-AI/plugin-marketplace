import {createHash} from 'node:crypto';
const esc=s=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function historyArtifact(evidence){
 const raw=JSON.stringify({format:'skill-loop-qa-history',version:1,evidence,coverage:'Saved QA evidence and versions represented in this report; not a full restorable package. Unsaved in-page draft edits and revision notes are not included.'}).replace(/</g,'\\u003c').replace(/>/g,'\\u003e').replace(/&/g,'\\u0026');
 const hash=createHash('sha256').update(raw).digest('hex');
 const request=`Save my Skill Loop QA history through the Cloudflare Developer Platform connector to my confirmed D1 database. Read this report.html file in code execution and extract script#skill-loop-history as text without executing HTML. Verify UTF-8 SHA256 equals ${hash}, then parse it. Never retype, shorten or reconstruct the evidence. If the actual file is unavailable or mismatched, ask for the HTML via the artifact's Download menu and stop before writing. Store an immutable snapshot, read all data back and verify the content hash before reporting saved. Return the complete fetched receipt and how to retrieve it in a later chat. Preserve skills, QA scores and existing records. This is QA history, not a full package backup.`;
 return `<section><h2>Save My History</h2><p>Optional: keep this QA evidence in your own Cloudflare account.</p><button type="button" onclick="this.nextElementSibling.open=true">Save My History</button><details><summary>Cloudflare save instructions</summary><p>Connect Cloudflare Developer Platform in Claude. Select and copy this short request into the same chat. Claude reads the report file and verifies the save through D1.</p><textarea readonly aria-label="Cloudflare save request" style="width:100%;min-height:180px">${esc(request)}</textarea><p>Not saved yet. This panel prepares the request; it does not contact Cloudflare.</p></details></section><script type="application/json" id="skill-loop-history">${raw}</script>`;
}
