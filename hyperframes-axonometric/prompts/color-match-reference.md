Match the current HyperFrames composition to the supplied reference image's palette and mood while preserving layout, labels, and functionality.

Do not rebuild geometry unless needed. Sample the reference palette locally if vision tools are unavailable. Apply concrete color tokens, glows, strokes, surfaces, and background changes. Then run lint, inspect, render, preview frames, and contact sheet.
