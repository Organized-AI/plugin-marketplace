Redesign the existing HyperFrames pass so it is materially more axonometric and architectural, without changing approved copy.

Focus only on visual architecture/motion unless copy changes are explicitly requested.
Add: cutaway slabs, bridge/walkway elements, glass volumes, grid underlay, columns/fins, measurement marks, stronger depth/shadows.
Remove or avoid: generic floating boxes, central medallions, orbit labels blocking the hero object, labels inside heavy perspective transforms.

After changes, run lint + inspect + render. Export preview frames and a contact sheet.
